## 1. Sink option

- [x] 1.1 `Options::versioned_producer` with `unversioned_producer()`; the sink writes the bare producer when it is off

## 2. Goldens

- [x] 2.1 Corpus runner, facade test, and marks golden test use the option
- [x] 2.2 Regenerate the goldens; verify the diff touches only the producer line and offsets
