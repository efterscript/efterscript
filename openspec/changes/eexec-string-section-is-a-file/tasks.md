## 1. The section

- [x] 1.1 `eexec` on a string opens the plaintext as a read-only file and runs it as a file source, so `currentfile` returns the section
- [x] 1.2 The operator's documentation says both operand forms produce a file

## 2. Coverage

- [x] 2.1 A corpus file asserts that a string section's `currentfile closefile` ends the section and the job continues
