%!PS
% SPDX-FileCopyrightText: 2026 EfterScript contributors
% SPDX-License-Identifier: MIT
% expect-output: done
% Scenario: a 200-level nest of loop bodies, each leaving its loop with exit,
% runs to completion within the default execution-stack limit.
% Expect: prints "done".
{ { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { {
{ { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { {
{ { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { {
{ { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { {
{ { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { { {
{ { { { { { { { { { { { { { { { { { { { exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop exit }
loop exit } loop exit } loop exit } loop exit } loop exit } loop
(done) =
