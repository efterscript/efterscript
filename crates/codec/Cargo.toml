# SPDX-FileCopyrightText: 2026 EfterScript contributors
# SPDX-License-Identifier: MIT

[package]
name = "codec"
description = "Byte codecs shared by the PDF writer and the interpreter's filters: DEFLATE, inflate, LZW, and the PNG and TIFF predictors. No dependencies, no I/O."
# Private-phase name; published crates use the `efterscript-*` prefix.
publish = false
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]

[dev-dependencies]
proptest = "1.11.0"
